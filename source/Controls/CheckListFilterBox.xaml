<UserControl x:Class="BackgroundChangerPlugin.Controls.CheckListFilterBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             SnapsToDevicePixels="True"
             Height="24">

    <UserControl.Resources>
        <DataTemplate x:Key="CheckListFilterItemTemplate">
            <CheckBox Content="{Binding Name}"
                      IsChecked="{Binding IsChecked, Mode=TwoWay}"
                      Padding="5,3,5,3"
                      Style="{DynamicResource ModernCheckBox}"
                      Checked="FilterItem_Changed"
                      Unchecked="FilterItem_Changed" />
        </DataTemplate>
    </UserControl.Resources>

    <Border x:Name="PART_RootBorder"
            BorderThickness="{DynamicResource ControlBorderThickness}"
            CornerRadius="{DynamicResource ControlCornerRadius}"
            Style="{DynamicResource HighlightBorder}">

        <Grid>
            <ToggleButton x:Name="MainToggle"
                          BorderThickness="0"
                          Focusable="True"
                          Background="Transparent"
                          HorizontalContentAlignment="Stretch"
                          VerticalContentAlignment="Stretch">
                <ToggleButton.Style>
                    <Style TargetType="ToggleButton">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="{x:Type ToggleButton}">
                                    <Border Background="Transparent">
                                        <ContentPresenter />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ToggleButton.Style>
            </ToggleButton>

            <DockPanel IsHitTestVisible="False">
                <Viewbox Width="16"
                         Height="16"
                         Margin="0,0,4,0"
                         DockPanel.Dock="Right"
                         VerticalAlignment="Center">
                    <TextBlock Text="6"
                               FontFamily="Marlett"
                               FontSize="11"
                               Foreground="{DynamicResource GlyphBrush}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center" />
                </Viewbox>

                <TextBlock x:Name="PART_TextFilterString"
                           Style="{DynamicResource BaseTextBlockStyle}"
                           TextTrimming="CharacterEllipsis"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Stretch"
                           DockPanel.Dock="Left"
                           Margin="6,0,0,0" />
            </DockPanel>

            <Popup x:Name="PART_DropdownPopup"
                   Placement="Bottom"
                   Focusable="False"
                   AllowsTransparency="True"
                   StaysOpen="False"
                   MaxHeight="300"
                   IsOpen="{Binding IsChecked, ElementName=MainToggle, Mode=TwoWay, Delay=100}"
                   MinWidth="{Binding ActualWidth, ElementName=MainToggle}">

                <Border Background="{DynamicResource PopupBackgroundBrush}"
                        BorderThickness="{DynamicResource PopupBorderThickness}"
                        BorderBrush="{DynamicResource PopupBorderBrush}"
                        CornerRadius="{DynamicResource ControlCornerRadius}">
                    <ScrollViewer MaxHeight="280"
                                  VerticalScrollBarVisibility="Auto">
                        <ItemsControl x:Name="PART_ItemsPanel"
                                      Margin="4"
                                      ItemTemplate="{StaticResource CheckListFilterItemTemplate}" />
                    </ScrollViewer>
                </Border>
            </Popup>
        </Grid>
    </Border>
</UserControl>
